{
  "name": "iceos-root",
  "private": true,
  "engines": { "node": "18.x" },
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npx --yes serve -s frontend/dist -l 3020"
  }
}


import EduNode from "./EduNode";

export const nodeTypes = Object.freeze({
  eduNode: EduNode,
});

export const defaultEdgeOptions = Object.freeze({
  type: "smoothstep",
  style: { stroke: "#22d3ee" },
});


